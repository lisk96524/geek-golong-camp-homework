question:
1. 我们在数据库操作的时候，比如 dao 层中当遇到一个 sql.ErrNoRows 的时候，是否应该 Wrap 这个 error，抛给上层。为什么，应该怎么做请写出代码?
听完课程，结合以往的经历，虽然没用过errnorows但是字面意思可以理解为查询返回了空行也就是没有数据的异常，那么该异常需要返回具体错误信息，要打上内容，交给调用者进行上层处理，所以需要wrap，我们需要告知上层调用者这里的具体原因。
代码参见errdeal.go